import "../../scene/SceneObject";

import "../../creature/CreatureObject";

import "../../player/Player";

import "../TangibleObject";

import "Pharmaceutical";

interface CureAreaPack implements Pharmaceutical {

	CureAreaPack(unsigned long oid, unsigned int tempCRC, const unicode n, const string tempn) {
		super(oid, tempCRC, n, tempn, int mptype);
	}

	CureAreaPack(CreatureObject creature, unsigned int tempCRC, const unicode n, const string tempn) {
		super(creature, tempCRC, n, tempn, int mptype);
	}

	void generateAttributes(SceneObject obj);

	void setEffectiveness(float eff);
	void setAreaOfEffect(float area);
	void setState(unsigned long condition);

	float getEffectiveness();
	unsigned long getState();
}
