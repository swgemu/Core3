/*
 *	server/zone/objects/area/NoBuildArea.cpp generated by engine3 IDL compiler 0.55
 */

#include "NoBuildArea.h"

#include "NoBuildAreaImplementation.h"

#include "Area.h"

/*
 *	NoBuildAreaStub
 */

NoBuildArea::NoBuildArea(Coordinate* center, float width, float height, unsigned char reason) : Area(DummyConstructorParameter::instance()) {
	_impl = new NoBuildAreaImplementation(center, width, height, reason);
	_impl->_setStub(this);
}

NoBuildArea::NoBuildArea(float minXPos, float maxXPos, float minYPos, float maxYPos, unsigned char reason) : Area(DummyConstructorParameter::instance()) {
	_impl = new NoBuildAreaImplementation(minXPos, maxXPos, minYPos, maxYPos, reason);
	_impl->_setStub(this);
}

NoBuildArea::NoBuildArea(DummyConstructorParameter* param) : Area(param) {
}

NoBuildArea::~NoBuildArea() {
}

unsigned char NoBuildArea::getType() {
	if (_impl == NULL) {
		if (!deployed)
			throw ObjectNotDeployedException(this);

		DistributedMethod method(this, 6);

		return method.executeWithUnsignedCharReturn();
	} else
		return ((NoBuildAreaImplementation*) _impl)->getType();
}

unsigned long long NoBuildArea::getReason() {
	if (_impl == NULL) {
		if (!deployed)
			throw ObjectNotDeployedException(this);

		DistributedMethod method(this, 7);

		return method.executeWithUnsignedLongReturn();
	} else
		return ((NoBuildAreaImplementation*) _impl)->getReason();
}

/*
 *	NoBuildAreaAdapter
 */

NoBuildAreaAdapter::NoBuildAreaAdapter(NoBuildAreaImplementation* obj) : AreaAdapter(obj) {
}

Packet* NoBuildAreaAdapter::invokeMethod(uint32 methid, DistributedMethod* inv) {
	Packet* resp = new MethodReturnMessage(0);

	switch (methid) {
	case 6:
		resp->insertByte(getType());
		break;
	case 7:
		resp->insertLong(getReason());
		break;
	default:
		return NULL;
	}

	return resp;
}

unsigned char NoBuildAreaAdapter::getType() {
	return ((NoBuildAreaImplementation*) impl)->getType();
}

unsigned long long NoBuildAreaAdapter::getReason() {
	return ((NoBuildAreaImplementation*) impl)->getReason();
}

/*
 *	NoBuildAreaHelper
 */

NoBuildAreaHelper* NoBuildAreaHelper::staticInitializer = NoBuildAreaHelper::instance();

NoBuildAreaHelper::NoBuildAreaHelper() {
	className = "NoBuildArea";

	DistributedObjectBroker::instance()->registerClass(className, this);
}

void NoBuildAreaHelper::finalizeHelper() {
	NoBuildAreaHelper::finalize();
}

DistributedObject* NoBuildAreaHelper::instantiateObject() {
	return new NoBuildArea(DummyConstructorParameter::instance());
}

DistributedObjectAdapter* NoBuildAreaHelper::createAdapter(DistributedObjectStub* obj) {
	DistributedObjectAdapter* adapter = new NoBuildAreaAdapter((NoBuildAreaImplementation*) obj->_getImplementation());

	obj->_setClassName(className);
	obj->_setClassHelper(this);

	adapter->setStub(obj);

	return adapter;
}

/*
 *	NoBuildAreaServant
 */

NoBuildAreaServant::NoBuildAreaServant(Coordinate* center, float width, float height) : AreaImplementation(center, width, height) {
	_classHelper = NoBuildAreaHelper::instance();
}

NoBuildAreaServant::NoBuildAreaServant(float minXPos, float maxXPos, float minYPos, float maxYPos) : AreaImplementation(minXPos, maxXPos, minYPos, maxYPos) {
	_classHelper = NoBuildAreaHelper::instance();
}

NoBuildAreaServant::~NoBuildAreaServant() {
}

void NoBuildAreaServant::_setStub(DistributedObjectStub* stub) {
	_this = (NoBuildArea*) stub;
	AreaServant::_setStub(stub);
}

DistributedObjectStub* NoBuildAreaServant::_getStub() {
	return _this;
}

