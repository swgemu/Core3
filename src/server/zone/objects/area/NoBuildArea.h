/*
 *	server/zone/objects/area/NoBuildArea.h generated by engine3 IDL compiler 0.55
 */

#ifndef NOBUILDAREA_H_
#define NOBUILDAREA_H_

#include "engine/orb/DistributedObjectBroker.h"

#include "engine/util/Coordinate.h"

class Area;

#include "Area.h"

class NoBuildArea : public Area {
public:
	NoBuildArea(Coordinate* center, float width, float height, unsigned char reason = 0);

	NoBuildArea(float minXPos, float maxXPos, float minYPos, float maxYPos, unsigned char reason = 0);

	unsigned char getType();

	unsigned long long getReason();

protected:
	NoBuildArea(DummyConstructorParameter* param);

	virtual ~NoBuildArea();

	friend class NoBuildAreaHelper;
};

class NoBuildAreaImplementation;

class NoBuildAreaAdapter : public AreaAdapter {
public:
	NoBuildAreaAdapter(NoBuildAreaImplementation* impl);

	Packet* invokeMethod(sys::uint32 methid, DistributedMethod* method);

	unsigned char getType();

	unsigned long long getReason();

};

class NoBuildAreaHelper : public DistributedObjectClassHelper, public Singleton<NoBuildAreaHelper> {
	static NoBuildAreaHelper* staticInitializer;

public:
	NoBuildAreaHelper();

	void finalizeHelper();

	DistributedObject* instantiateObject();

	DistributedObjectAdapter* createAdapter(DistributedObjectStub* obj);

	friend class SingletonWrapper<NoBuildAreaHelper>;
};

#include "AreaImplementation.h"

class NoBuildAreaServant : public AreaImplementation {
public:
	NoBuildArea* _this;

public:
	NoBuildAreaServant(Coordinate* center, float width, float height);
	NoBuildAreaServant(float minXPos, float maxXPos, float minYPos, float maxYPos);
	virtual ~NoBuildAreaServant();

	void _setStub(DistributedObjectStub* stub);
	DistributedObjectStub* _getStub();

};

#endif /*NOBUILDAREA_H_*/
