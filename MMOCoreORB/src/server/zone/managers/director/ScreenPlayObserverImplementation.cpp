/*
				Copyright <SWGEmu>
		See file COPYING for copying conditions. " + panic.getMessage() + " while trying to run SceenPlayObserver: " + play + ":" + key);
	}

	//1 remove observer, 0 keep observer

	return ret;
}
