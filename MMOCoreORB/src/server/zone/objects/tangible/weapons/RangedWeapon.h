/*
 *	server/zone/objects/tangible/weapons/RangedWeapon.h generated by engine3 IDL compiler 0.55
 */

#ifndef RANGEDWEAPON_H_
#define RANGEDWEAPON_H_

#include "engine/orb/DistributedObjectBroker.h"

class CreatureObject;

class Weapon;

#include "Weapon.h"

class RangedWeapon : public Weapon {
public:
	RangedWeapon(unsigned long long oid, unsigned int tempCRC, const UnicodeString& n, const String& tempn, int tp, bool eqp = false);

	RangedWeapon(CreatureObject* creature, const String& temp, const UnicodeString& n, const String& tempn, int tp, bool eqp = false);

	int getRange();

	void setRange(int rng);

protected:
	RangedWeapon(DummyConstructorParameter* param);

	virtual ~RangedWeapon();

	friend class RangedWeaponHelper;
};

class RangedWeaponImplementation;

class RangedWeaponAdapter : public WeaponAdapter {
public:
	RangedWeaponAdapter(RangedWeaponImplementation* impl);

	Packet* invokeMethod(sys::uint32 methid, DistributedMethod* method);

	int getRange();

	void setRange(int rng);

};

class RangedWeaponHelper : public DistributedObjectClassHelper, public Singleton<RangedWeaponHelper> {
	static RangedWeaponHelper* staticInitializer;

public:
	RangedWeaponHelper();

	void finalizeHelper();

	DistributedObject* instantiateObject();

	DistributedObjectAdapter* createAdapter(DistributedObjectStub* obj);

	friend class SingletonWrapper<RangedWeaponHelper>;
};

#include "WeaponImplementation.h"

class RangedWeaponServant : public WeaponImplementation {
public:
	RangedWeapon* _this;

public:
	RangedWeaponServant(unsigned long long oid, unsigned int tempCRC, const UnicodeString& n, const String& tempn, bool eqp, int tp, int cat);
	RangedWeaponServant(CreatureObject* creature, const String& temp, const UnicodeString& n, const String& tempn, bool eqp, int tp, int cat);
	virtual ~RangedWeaponServant();

	void _setStub(DistributedObjectStub* stub);
	DistributedObjectStub* _getStub();

};

#endif /*RANGEDWEAPON_H_*/
