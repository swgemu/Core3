/*
 *	server/zone/objects/tangible/weapons/ranged/HeavyRangedWeapon.h generated by engine3 IDL compiler 0.55
 */

#ifndef HEAVYRANGEDWEAPON_H_
#define HEAVYRANGEDWEAPON_H_

#include "engine/orb/DistributedObjectBroker.h"

class CreatureObject;

class Player;

class RangedWeapon;

#include "../RangedWeapon.h"

class HeavyRangedWeapon : public RangedWeapon {
public:
	HeavyRangedWeapon(unsigned long long oid, unsigned int tempCRC, const UnicodeString& n, const String& tempn, bool eqp = false);

	HeavyRangedWeapon(CreatureObject* creature, const String& temp, const UnicodeString& n, const String& tempn, bool eqp = false);

	int useObject(Player* player);

	void activateSkill(Player* player);

	void setSkill(const String& sk);

	String& getSkill();

	unsigned int getSkillCRC();

	void useCharge(Player* player);

	void sendDeltas(Player* player);

protected:
	HeavyRangedWeapon(DummyConstructorParameter* param);

	virtual ~HeavyRangedWeapon();

	String _return_getSkill;

	friend class HeavyRangedWeaponHelper;
};

class HeavyRangedWeaponImplementation;

class HeavyRangedWeaponAdapter : public RangedWeaponAdapter {
public:
	HeavyRangedWeaponAdapter(HeavyRangedWeaponImplementation* impl);

	Packet* invokeMethod(sys::uint32 methid, DistributedMethod* method);

	int useObject(Player* player);

	void activateSkill(Player* player);

	void setSkill(const String& sk);

	String& getSkill();

	unsigned int getSkillCRC();

	void useCharge(Player* player);

	void sendDeltas(Player* player);

protected:
	String _param0_setSkill__String_;
};

class HeavyRangedWeaponHelper : public DistributedObjectClassHelper, public Singleton<HeavyRangedWeaponHelper> {
	static HeavyRangedWeaponHelper* staticInitializer;

public:
	HeavyRangedWeaponHelper();

	void finalizeHelper();

	DistributedObject* instantiateObject();

	DistributedObjectAdapter* createAdapter(DistributedObjectStub* obj);

	friend class SingletonWrapper<HeavyRangedWeaponHelper>;
};

#include "../RangedWeaponImplementation.h"

class HeavyRangedWeaponServant : public RangedWeaponImplementation {
public:
	HeavyRangedWeapon* _this;

public:
	HeavyRangedWeaponServant(unsigned long long oid, unsigned int tempCRC, const UnicodeString& n, const String& tempn, int tp, bool eqp);
	HeavyRangedWeaponServant(CreatureObject* creature, const String& temp, const UnicodeString& n, const String& tempn, int tp, bool eqp);
	virtual ~HeavyRangedWeaponServant();

	void _setStub(DistributedObjectStub* stub);
	DistributedObjectStub* _getStub();

};

#endif /*HEAVYRANGEDWEAPON_H_*/
