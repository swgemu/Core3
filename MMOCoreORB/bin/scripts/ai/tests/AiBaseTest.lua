includeFile = spy.new(function() end)
require("ai.ai")

describe("Basic Behavior Tests", function()
  it("Defines the Task Object", function()
    assert.is.truthy(Task)
  end)
  it("Defines the Action Object", function()
    assert.is.truthy(Action)
  end)
  it("Defines the Template Object", function()
    assert.is.truthy(CreatureAiTemplate)
  end)

end)
