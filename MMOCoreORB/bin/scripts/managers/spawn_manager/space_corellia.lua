-- {"regionName", xCenter, yCenter, radius, tier, ["spawnGroup1"], [...]}
-- tier: 0 = no spawn, 1 = static spawn area, 2 = dynamic spawn area

space_corellia_regions = {}

space_corellia_static_spawns = { }

space_corellia_badges = {
}
